<h2>My tasks</h2>

<ul>
  <% @tasks.each do |task| %>
    <li><input type="checkbox">  <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%= link_to "Create new task", tasks_new_path %>
